NMN Growth Desk — static HTML export

Upload every file and folder in this directory to the root of a static web host.
Weekly review: /index.html
Ads console: /admin/index.html

The dashboard is self-contained and does not send report data to an external API.
Date filters, hierarchy drill-down, comparisons and demographic tabs run in the browser.